---
phase: 17-ui-polish-bug-hunt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md
  - packages/web/app/components/InterventionCard.vue
  - packages/web/app/assets/css/main.css
  - packages/web/package.json
  - pnpm-lock.yaml
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/10-ingestion-acteurs-organes/10-03-PLAN.md
  - packages/ingestion/scripts/ingest_debates.py
  - packages/ingestion/scripts/ingest_debates_senat.py
autonomous: false
must_haves:
  truths:
    - "Phase 16 carryover (InterventionCard table rendering, main.css table styles, package.json, pnpm-lock.yaml, ingestion/planning edits) is committed on dev branch as a chore commit — working tree is clean of those 9 files before audit starts."
    - "A punch list file .planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md exists and enumerates every bug/polish issue found across all 9 routes (/, /debates/[id], /deputies, /deputies/[id], /votes, /votes/[id], /search, /about, /legal) at 3 viewports (1280, 768, 390). Note: / IS the debates index — there is no separate /debates route."
    - "Every punch list entry references a specific file + line range, has a severity (blocker/major/minor/nit), a category (layout/overflow/spacing/typo/responsive/DA/content/bug), and an assignment tag (17-02 or 17-03)."
    - "The known issues from 17-RESEARCH.md (voteStats DA violation, mobile nav hidden, tag filter reset on /deputies/[id] pagination, about.vue Sénat coverage, legal.vue hosting placeholder, debate detail full-payload load) are all present in the punch list."
    - "User has reviewed and approved the punch list before 17-02 and 17-03 start (checkpoint)."
  artifacts:
    - path: ".planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md"
      provides: "Concrete bug punch list grouped by page, with severity + category + owning plan (17-02 or 17-03)"
      contains: "## / (home)"
  key_links:
    - from: "17-01-PUNCHLIST.md"
      to: "17-02 + 17-03 task scopes"
      via: "assignment tag per entry"
      pattern: "owner: 17-0[23]"
---

<objective>
Clear the uncommitted Phase 16 carryover so Phase 17 starts from a clean tree, then perform a systematic visual + functional audit of every user-facing page at 3 viewports and produce a concrete, file-anchored punch list that plans 17-02 and 17-03 will execute against.

Purpose: Without a factual bug inventory, 17-02 degenerates into ad-hoc tweaking. The punch list becomes the contract for the polish phase.
Output: One chore commit + one punch list markdown file + user approval checkpoint.
</objective>

<execution_context>
@C:/Users/Dharma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dharma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-polish-bug-hunt/17-RESEARCH.md
@packages/web/app/components/InterventionCard.vue
@packages/web/app/assets/css/main.css
@packages/web/app/layouts/default.vue
@packages/web/app/pages/index.vue
@packages/web/app/pages/debates/index.vue
@packages/web/app/pages/debates/[id].vue
@packages/web/app/pages/deputies/index.vue
@packages/web/app/pages/deputies/[id].vue
@packages/web/app/pages/votes/index.vue
@packages/web/app/pages/votes/[id].vue
@packages/web/app/pages/search.vue
@packages/web/app/pages/about.vue
@packages/web/app/pages/legal.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit Phase 16 carryover as chore</name>
  <files>
    packages/web/app/components/InterventionCard.vue
    packages/web/app/assets/css/main.css
    packages/web/package.json
    pnpm-lock.yaml
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/10-ingestion-acteurs-organes/10-03-PLAN.md
    packages/ingestion/scripts/ingest_debates.py
    packages/ingestion/scripts/ingest_debates_senat.py
  </files>
  <action>
    From the repo root on branch `dev`:
    1. Run `git status` and `git diff --stat` to confirm the 9 files listed above are the only uncommitted changes (plus the untracked .claude/ directory, which MUST NOT be committed).
    2. Inspect each file change with `git diff -- <file>` to verify intent: InterventionCard.vue + main.css should be the DOMPurify table rendering work from Phase 16, package.json + pnpm-lock.yaml should be the isomorphic-dompurify dependency, ingestion scripts + planning docs should be the Phase 16 closure edits.
    3. If any file's diff looks unrelated to Phase 16 closure, STOP and report to user — do not commit speculatively.
    4. Stage the 9 files explicitly by name (NOT `git add -A` and NOT `git add .`):
       `git add packages/web/app/components/InterventionCard.vue packages/web/app/assets/css/main.css packages/web/package.json pnpm-lock.yaml .planning/ROADMAP.md .planning/STATE.md .planning/phases/10-ingestion-acteurs-organes/10-03-PLAN.md packages/ingestion/scripts/ingest_debates.py packages/ingestion/scripts/ingest_debates_senat.py`
    5. Create a single chore commit with a HEREDOC message titled `chore(phase-17): carry over phase 16 residual edits before audit` and a body explaining this is pre-audit housekeeping — InterventionCard DOMPurify table rendering, main.css table styles, isomorphic-dompurify dependency bump, ingestion + planning edits from Phase 16 closure — so that Phase 17 starts from a clean working tree. Include the Co-Authored-By trailer.
    6. Run `git status` after — working tree should show only `.claude/` as untracked and nothing modified.
    DO NOT push. DO NOT amend. DO NOT touch `.claude/`.
  </action>
  <verify>
    `git status --porcelain | grep -v '^?? .claude/'` returns empty.
    `git log -1 --format=%s` starts with `chore(phase-17):`.
    `git show --stat HEAD` lists exactly the 9 expected files.
  </verify>
  <done>
    Clean working tree (excluding untracked .claude/), one new chore commit on dev containing only the 9 carryover files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Systematic 9-page audit + punch list generation</name>
  <files>
    .planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md
  </files>
  <action>
    Boot the dev server (`pnpm --filter @poliscope/web dev` in background, wait for `Listening on http://localhost:3000`). Then systematically audit every page at 3 viewports (desktop 1280x800, tablet 768x1024, mobile 390x844) using a combination of:
      - static code review (read each page.vue + its used components)
      - the agent-browser WSL wrapper (`C:/Users/Dharma/.local/bin/agent-browser.sh navigate http://localhost:3000/<route>`, then `snapshot` and `screenshot` at each viewport) — prefer this over WebFetch because pages are JS-rendered
      - the existing Playwright suite output for signals (`pnpm --filter @poliscope/web test:e2e` can be skipped here — just read the spec files to understand current coverage gaps)

    Routes to audit (in order): `/` (home IS the debates list — there is no /debates route), `/debates/<first-id>`, `/deputies`, `/deputies/<first-id>`, `/votes`, `/votes/<first-id>`, `/search` (empty state + with a query like "climat"), `/about`, `/legal`.

    For each page capture:
      - Layout issues (overflow-x, broken grid, cramped spacing, misaligned elements)
      - Typography issues (wrong font, wrong size, wrapping/truncation bugs, hardcoded French strings that should be dynamic)
      - Responsive issues (content hidden or unreachable at 768 / 390, nav behavior, card wrapping)
      - DA violations (any color outside marble/bronze/ink/terracotta, any `shadow-*` class, any font other than Cinzel/Inter)
      - Content staleness (about.vue mentions only Assemblée nationale but app now covers Sénat; legal.vue hosting "À définir.")
      - Functional/display bugs (the known `/deputies/[id]` tag filter reset on pagination; the `/debates/[id]` monolithic full-payload load; any other broken interaction surfaced during audit)
      - Console errors (open devtools via agent-browser or note any hydration warnings visible in dev server logs)

    Write findings to `.planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md` using this exact structure:

    ```markdown
    # Phase 17 Punch List

    Generated: <YYYY-MM-DD>
    Audit viewports: 1280x800, 768x1024, 390x844
    Dev server: http://localhost:3000
    Git HEAD at audit: <short-sha>

    ## Legend
    - Severity: blocker | major | minor | nit
    - Category: layout | overflow | spacing | typo | responsive | DA | content | bug
    - Owner: 17-02 (bug fixes + InterventionCard) | 17-03 (debate detail pagination)

    ## Global (layout/default.vue, app.vue, shared components)
    - [ ] **[major][responsive][17-02]** Mobile nav hidden by `hidden sm:block` — nav links unreachable at 390px. File: `packages/web/app/layouts/default.vue:L<line>`. Fix: remove `hidden sm:block`, let links wrap with smaller text.
    - [ ] ...

    ## / (home / pages/index.vue) — NOTE: / IS the debates index, there is no separate /debates route
    - [ ] ...

    ## /debates/[id] (pages/debates/[id].vue)
    - [ ] **[blocker][bug][17-03]** API returns ALL interventions in one payload — slow page + large DOM. File: `packages/web/server/api/debates/[id].get.ts` + `packages/web/app/pages/debates/[id].vue`. Fix: paginate (clone pattern from deputies/[id].vue).
    - [ ] **[major][DA][17-02]** InterventionCard: consecutive speakers not grouped, role accent missing, orderInDebate never displayed, tag pills never displayed. File: `packages/web/app/components/InterventionCard.vue` + parent page.
    - [ ] ...

    ## /deputies (pages/deputies/index.vue)
    - [ ] ...

    ## /deputies/[id] (pages/deputies/[id].vue)
    - [ ] **[major][bug][17-02]** Tag filter reset on pagination — `watch(page, () => { activeTag.value = null })` wipes the active tag every page change. File: `packages/web/app/pages/deputies/[id].vue:L<line>`. Fix: remove the reset.
    - [ ] **[major][DA][17-02]** voteStats uses `bg-green-100 text-green-800` + `bg-red-100` — outside marble/bronze/ink/terracotta palette. File: `packages/web/app/pages/deputies/[id].vue:L<line>`. Fix: text-ink / text-bronze / text-terracotta / bg-marble-dark.
    - [ ] ...

    ## /votes (pages/votes/index.vue)
    - [ ] ...

    ## /votes/[id] (pages/votes/[id].vue)
    - [ ] ...

    ## /search (pages/search.vue)
    - [ ] ...

    ## /about (pages/about.vue)
    - [ ] **[minor][content][17-02]** Copy only mentions Assemblée nationale — app now covers Sénat. File: `packages/web/app/pages/about.vue:L<line>`. Fix: rewrite to mention both chambers.

    ## /legal (pages/legal.vue)
    - [ ] **[minor][content][17-02]** Hébergement: "À définir." — we know it's LXC 200 + nginx + PM2 (Proxmox homelab). File: `packages/web/app/pages/legal.vue:L<line>`. Fix: fill in real hosting info.

    ## Summary
    - 17-02 items: <count>
    - 17-03 items: <count>
    - Blockers: <count> | Majors: <count> | Minors: <count> | Nits: <count>
    ```

    Rules while writing the punch list:
      - Every entry MUST have file + line reference (use `:L<line>` or `:L<start>-L<end>` ranges).
      - Every entry MUST have severity, category, owner tags in the `[x][y][z]` prefix format.
      - Pre-seed the 6 known-issues from 17-RESEARCH.md (voteStats DA, mobile nav, tag filter reset, about copy, legal copy, debate detail pagination) even if the audit would have found them anyway — they are required.
      - Do NOT fix anything in this task. Punch list only.
      - If a page is clean at all 3 viewports, write `- _No issues found at 1280/768/390._` under its heading.
      - Kill the background dev server when done.
  </action>
  <verify>
    `test -f .planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md` succeeds.
    File contains headings for all 10 sections (Global + 9 routes: /, /debates/[id], /deputies, /deputies/[id], /votes, /votes/[id], /search, /about, /legal).
    All 6 pre-seeded known issues are present (grep for: `voteStats`, `hidden sm:block`, `activeTag.value = null`, `Assemblée nationale`, `À définir`, `paginate`/`pagination`).
    Every non-empty list item matches the `[severity][category][owner]` prefix format.
    Summary counts at bottom are non-zero.
    No code files modified in this task (punch list only) — `git status` shows only the new punch list file as changed.
  </verify>
  <done>
    Punch list file exists, is comprehensive, file-anchored, and correctly tagged. Dev server stopped. Ready for user review.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Chore commit landed on dev with the 9 Phase 16 carryover files.
    - Punch list at `.planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md` enumerating every bug/polish issue across all 9 pages at 3 viewports, with severity/category/owner tags and file:line references.
  </what-built>
  <how-to-verify>
    1. `git log -1` — confirm the chore commit is there and looks right.
    2. `git status` — should show only `.claude/` as untracked, nothing modified.
    3. Open `.planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md` and scan it end to end.
    4. Sanity check a few entries — are the file:line refs accurate? Is the severity triage sane?
    5. Check the summary counts at the bottom — do they look reasonable (not 2 items total, not 400)?
    6. Flag anything missing that you know should be in there. Approve, or ask Claude to re-audit specific routes before 17-02/17-03 start.
  </how-to-verify>
  <resume-signal>Type "approved" to unlock Wave 2 (17-02 + 17-03), or list corrections needed to the punch list.</resume-signal>
</task>

</tasks>

<verification>
- Clean git tree (excluding `.claude/`) after Task 1.
- Punch list file exists, well-formed, tagged, file-anchored after Task 2.
- Pre-seeded 6 known issues present.
- User approval received before Phase 17 Wave 2 proceeds.
</verification>

<success_criteria>
Wave 2 plans (17-02, 17-03) have a concrete, reviewed, approved work list to execute against. No bug hunting mid-fix.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-polish-bug-hunt/17-01-SUMMARY.md`
</output>
