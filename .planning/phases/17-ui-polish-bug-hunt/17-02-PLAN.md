---
phase: 17-ui-polish-bug-hunt
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - packages/web/app/components/InterventionCard.vue
  - packages/web/app/pages/debates/[id].vue
  - packages/web/app/pages/deputies/[id].vue
  - packages/web/app/pages/votes/index.vue
  - packages/web/app/layouts/default.vue
  - packages/web/app/pages/about.vue
  - packages/web/app/pages/legal.vue
  - packages/web/app/assets/css/main.css
autonomous: true
must_haves:
  truths:
    - "InterventionCard.vue accepts an `isContinuation?: boolean` prop that, when true, suppresses the avatar + speaker header row and applies tighter top spacing. The card-internal rendering behavior is owned by 17-02. The parent-side computation of the flag (comparing current/previous deputyId over the accumulator array) is owned by 17-03 Task 2 and wired from `packages/web/app/pages/debates/[id].vue`. The combined end-state: consecutive interventions by the same non-null deputyId render grouped."
    - "Interventions by roles matching presidential/rapporteur/ministre keywords (président, présidente, rapporteur, rapporteure, ministre, secrétaire d'État) render with a bronze left accent (`border-l-2 border-bronze pl-3`) inside InterventionCard."
    - "InterventionCard displays `#N` (orderInDebate) in the card header and the top 3 tag pills at the bottom of the card body — data already present in props but previously unrendered."
    - "voteStats on /deputies/[id] no longer uses any `bg-green-*`, `text-green-*`, `bg-red-*`, or `text-red-*` classes — only marble/bronze/ink/terracotta palette tokens."
    - "On /deputies/[id], clicking a tag then paginating (load more) KEEPS the active tag filter — the `watch(page, () => { activeTag.value = null })` reset is gone."
    - "Mobile (390px) shows nav links in the default layout header — no `hidden sm:block` hiding them; links wrap with smaller text if needed."
    - "about.vue text mentions both Assemblée nationale and Sénat explicitly; legal.vue hosting section reflects real hosting (LXC Proxmox + nginx + PM2) instead of 'À définir.'"
    - "Every punch list item tagged `owner: 17-02` in 17-01-PUNCHLIST.md is either fixed (checkbox ticked with a short note) or explicitly deferred with a reason."
    - "No new `shadow-*` classes introduced. No color tokens outside marble/bronze/ink/terracotta introduced. No fonts other than Cinzel/Inter introduced."
  artifacts:
    - path: "packages/web/app/components/InterventionCard.vue"
      provides: "InterventionCard with isContinuation prop (card-internal rendering), bronze role accent, orderInDebate display, tag pills, data-testid=intervention-card on root"
      contains: "isContinuation"
    - path: "packages/web/app/pages/debates/[id].vue"
      provides: "Non-intervention-render fixes from punch list (does NOT compute isContinuation — 17-03 owns parent wiring)"
    - path: "packages/web/app/pages/deputies/[id].vue"
      provides: "voteStats using DA palette + tag filter persists across pagination"
      contains: "text-bronze"
    - path: "packages/web/app/layouts/default.vue"
      provides: "Mobile-visible nav (no hidden sm:block on nav links)"
    - path: "packages/web/app/pages/about.vue"
      provides: "Copy mentioning both chambers"
      contains: "Sénat"
    - path: "packages/web/app/pages/legal.vue"
      provides: "Real hosting info"
  key_links:
    - from: "packages/web/app/components/InterventionCard.vue"
      to: "speakerRole keyword detection"
      via: "computed bronze accent class"
      pattern: "border-l-2 border-bronze"
    # Note: the :is-continuation parent->child binding lives in plan 17-03 (parent wiring), not here.
---

<objective>
Execute every punch list item in `17-01-PUNCHLIST.md` tagged `owner: 17-02`, with the headline work being the InterventionCard refactor (consecutive speaker grouping, bronze role accent, orderInDebate display, tag pills) — all within the DataCommune DA (marble/bronze/ink/terracotta, Cinzel+Inter, no shadows).

Purpose: The InterventionCard is where debate readability lives. Every other fix in this plan is a DA/copy/bug sweep that together make the app feel polished.
Output: InterventionCard refactor + voteStats DA fix + tag filter bug fix + mobile nav fix + content refreshes + all 17-02 punch list checkboxes ticked.
</objective>

<execution_context>
@C:/Users/Dharma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dharma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-ui-polish-bug-hunt/17-RESEARCH.md
@.planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md
@packages/web/app/components/InterventionCard.vue
@packages/web/app/pages/debates/[id].vue
@packages/web/app/pages/deputies/[id].vue
@packages/web/app/layouts/default.vue
@packages/web/app/pages/about.vue
@packages/web/app/pages/legal.vue
@packages/web/app/assets/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: InterventionCard refactor — grouping, role accent, orderInDebate, tag pills</name>
  <files>
    packages/web/app/components/InterventionCard.vue
    packages/web/app/pages/debates/[id].vue
    packages/web/app/assets/css/main.css
  </files>
  <action>
    Refactor `packages/web/app/components/InterventionCard.vue` to increase intervention differentiation without drifting from the DataCommune DA. Four changes, all inside the existing template + script setup:

    1. **`isContinuation` prop (consecutive speaker grouping) — card-internal only**
       - Add `isContinuation?: boolean` to the component's defineProps (default false).
       - When `isContinuation` is true: suppress the entire speaker header row (avatar + speaker name + role + orderInDebate line). Keep only the body + footer of the card. Reduce top padding to `pt-1` and remove the top border on the card (or tighten spacing — whichever reads as "continuation of the previous card" without adding shadows).
       - **OWNERSHIP NOTE:** This task does NOT wire the prop from the parent. Plan 17-03 Task 2 owns the parent-side v-for and the per-element `isContinuation` computation (because 17-03 creates the `interventions` accumulator ref that is the source of truth for the comparison). 17-02 Task 1 does NOT touch the `<template>` v-for loop or the script-setup data-loading block in `packages/web/app/pages/debates/[id].vue` — only InterventionCard.vue for the continuation behavior.
       - `packages/web/app/pages/debates/[id].vue` remains in this task's `<files>` ONLY for the sub-step below (adding `data-testid="intervention-card"` if it ends up belonging on the wrapper and for any non-intervention-rendering fixes from Task 2). The isContinuation computation itself is 17-03's territory.

    2. **Bronze role accent for important speakers**
       - Add a computed `hasRoleAccent` in InterventionCard.vue that returns true if `speakerRole` (case-insensitive, diacritics-stripped) matches ANY of: `président`, `présidente`, `rapporteur`, `rapporteure`, `ministre`, `secrétaire d'état`. Use a regex with `/i` flag OR a small keyword list with `.toLowerCase().normalize('NFD').replace(/\p{Diacritic}/gu,'')` then `.includes(...)`.
       - When true, add `border-l-2 border-bronze pl-3` to the outermost card wrapper (inside the existing classes, not replacing them). Do not add shadows. Do not change background color.
       - Accent must still render on continuation cards (it's per-intervention, not per-speaker-run).

    3. **orderInDebate display**
       - In the card header row (only when NOT `isContinuation`), render `#{{ orderInDebate }}` next to the speaker name — small muted text (`text-xs text-ink/60` or equivalent existing muted class in the file). Data is already in props per 17-RESEARCH.md.

    3b. **data-testid="intervention-card" on root element**
       - Add `data-testid="intervention-card"` to the outermost wrapper of `InterventionCard.vue` (the same element that receives the DA classes). Plan 17-04's `debate-pagination.spec.ts` depends on this selector. Must be present regardless of `isContinuation` state.

    4. **Top 3 tag pills**
       - Below the intervention body (before the footer meta row), render up to 3 tag pills from the existing `tags` prop. Use a pill style consistent with the rest of the app (check SearchResultCard.vue for the exact classes — `bg-marble-dark text-ink/80 text-xs px-2 py-0.5 rounded-full` or whatever the existing pattern is). If `tags` is empty or undefined, render nothing (no empty container).
       - Suppress tag pills entirely when `isContinuation` is true (continuation cards should feel minimal).

    Constraints (hard):
      - No shadows. No new colors outside marble/bronze/ink/terracotta. No new fonts.
      - Don't break the existing DOMPurify intervention HTML rendering from Phase 16.
      - Don't remove the `data-testid` attributes that existing Playwright tests rely on (grep `data-testid` in e2e/ first to know which to preserve).
      - If `main.css` needs a tiny rule for continuation spacing (e.g., adjacent-sibling top-border removal), add it in the existing `.intervention-*` scope — don't create a new top-level class system.

    After coding, manually verify by running the dev server and visiting a debate detail page (first one from /debates). Check: consecutive speakers group, a président/ministre row has bronze left accent, `#N` shows, tag pills show at the bottom of non-continuation cards.
  </action>
  <verify>
    - `grep -n "isContinuation" packages/web/app/components/InterventionCard.vue` — prop defined + used.
    - `grep -n "isContinuation\|is-continuation" packages/web/app/pages/debates/\[id\].vue` — parent computes + passes.
    - `grep -n "border-bronze" packages/web/app/components/InterventionCard.vue` — accent class present.
    - `grep -n "orderInDebate" packages/web/app/components/InterventionCard.vue` — rendered in template.
    - `grep -nE "tags\.slice\(0, ?3\)|tags\.slice\(0,3\)" packages/web/app/components/InterventionCard.vue` — top 3 tags rendered.
    - `grep -n "data-testid=\"intervention-card\"" packages/web/app/components/InterventionCard.vue` — present on root wrapper.
    - No new `shadow-` or `bg-green`/`bg-red`/`text-green`/`text-red` introduced: `grep -nE "shadow-|bg-(green|red)|text-(green|red)" packages/web/app/components/InterventionCard.vue packages/web/app/pages/debates/\[id\].vue packages/web/app/assets/css/main.css` returns nothing new.
    - Dev server renders a debate detail page without hydration warnings (check terminal output).
  </verify>
  <done>
    InterventionCard visually differentiates consecutive speakers, marks presidential/rapporteur/ministre rows with bronze left accent, shows #N and up to 3 tag pills, and respects null deputyId (no grouping for unknowns). DA untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep — mobile nav, voteStats DA, tag filter bug, content refresh, remaining 17-02 punch list</name>
  <files>
    packages/web/app/layouts/default.vue
    packages/web/app/pages/deputies/[id].vue
    packages/web/app/pages/votes/index.vue
    packages/web/app/pages/about.vue
    packages/web/app/pages/legal.vue
    .planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md
  </files>
  <action>
    Walk the punch list top to bottom and fix every item tagged `owner: 17-02`. The 5 known-issue fixes are below — any additional items surfaced during audit MUST also be fixed in this task (they're all small; if one turns out to be non-trivial, leave a TODO comment + defer in the punch list with a short reason).

    1. **Mobile nav** (`packages/web/app/layouts/default.vue`)
       - Find the nav element with class `hidden sm:block` (or similar). Remove `hidden sm:block` so nav is visible at all viewports.
       - If links overflow at 390px, apply `flex-wrap gap-2 text-sm` (or tighten via existing classes). No JS toggle — static wrap only.
       - Verify at 390px via agent-browser or browser devtools that Home / Débats / Députés / Votes / Recherche / À propos links are all clickable and don't clip the header.

    2. **voteStats DA fix** (`packages/web/app/pages/deputies/[id].vue`)
       - Find the `voteStats` rendering section. Replace any `bg-green-100`, `bg-green-*`, `text-green-*`, `bg-red-100`, `bg-red-*`, `text-red-*` classes.
       - Replacement palette: use `text-bronze` for "pour" (positive/affirmative), `text-terracotta` for "contre" (negative), `text-ink/70` for "abstention", and `bg-marble-dark` for any chip background. Keep layout + spacing identical — this is a color-only swap.
       - If there are count labels, keep them in Inter, not Cinzel.

    3. **Tag filter pagination bug** (`packages/web/app/pages/deputies/[id].vue`)
       - Find `watch(page, () => { activeTag.value = null })` (or the equivalent watcher that clears activeTag on page change).
       - Remove that watcher entirely — paginating should NOT reset the active tag filter.
       - If the original intent was "reset to page 1 when a tag changes," that's a separate watch on `activeTag` → reset `page`, which is fine and should stay if present. Only remove the watcher that wipes the tag when page changes.

    3b. **data-testid additions for 17-04 regression guards**
       - In `packages/web/app/pages/deputies/[id].vue`, add `data-testid="tag-filter-pill"` to each rendered tag pill element (the clickable one that sets `activeTag`). If there's a v-for, the attribute goes on the element inside the loop — every pill gets it.
       - In `packages/web/app/pages/deputies/[id].vue`, add `data-testid="deputy-load-more"` to the existing load-more button.
       - In `packages/web/app/pages/votes/index.vue`, add `data-testid="vote-card"` to the outermost wrapper of each vote card element (the same element that wraps the `<NuxtLink>` to `/votes/[id]`, or the `<NuxtLink>` itself if the card IS the link). Match the pattern of existing `data-testid="debate-card"` and `data-testid="deputy-card"` in the sibling files so 17-04 specs can locate them consistently.
       - These are testability-enablement edits with zero DA impact. No classes change.

    4. **about.vue content refresh**
       - Rewrite the body copy to explicitly mention both `Assemblée nationale` AND `Sénat` as data sources. Keep the DA (Cinzel headings, Inter body). 2-3 sentences max. Factually honest about what the app covers.

    5. **legal.vue hosting fix**
       - Replace the `À définir.` placeholder in the Hébergement section with real info: hosted on a LXC container on a Proxmox VE homelab (France), served by nginx as reverse proxy, Node.js via PM2. If there's an existing Contact/Éditeur section with other "À définir" placeholders, fix those too (use the git user `Ianis` as editor if the file already leans that way — but do NOT invent a physical address or a personal email if one isn't already established in repo). If unsure what to put, leave a specific TODO comment with what's needed.

    6. **Remaining 17-02 punch list items**
       - For each remaining `[ ] ... [17-02]` checkbox in `17-01-PUNCHLIST.md`, apply the fix described in the entry's file:line reference. Tick the checkbox to `[x]` and append ` — fixed: <1-line note>` to the line.
       - For any item you decide NOT to fix (scope/risk), change `[ ]` to `[~]` and append ` — deferred: <reason>`. Do not delete items.

    Constraints (hard):
      - No `shadow-*`. No colors outside marble/bronze/ink/terracotta. No fonts outside Cinzel/Inter.
      - No new dependencies.
      - Don't touch `packages/web/server/api/debates/[id].ts` or the debate detail pagination — that's 17-03's exclusive territory.
      - Don't touch `packages/web/app/pages/debates/[id].vue` intervention rendering OR the parent-side `:is-continuation` wiring — 17-03 owns the v-for and the parent-side computation. You MAY touch debates/[id].vue for unrelated punch-list issues, but coordinate via file section not line collision.
      - InterventionCard.vue is Task 1's exclusive territory within this plan — don't touch it in Task 2.
      - `packages/web/app/pages/votes/index.vue` is in this task's scope ONLY for adding `data-testid="vote-card"` (sub-step 3b) and any non-DA-breaking punch-list fixes for the votes list page.

    After coding, re-run the dev server and spot-check:
      - Mobile viewport shows nav links.
      - /deputies/<someone-with-votes> shows DA-consistent vote stats.
      - /deputies/<someone-with-tags> → click a tag → paginate → tag still active.
      - /about mentions Sénat.
      - /legal hosting section no longer says "À définir.".
  </action>
  <verify>
    - `grep -n "hidden sm:block" packages/web/app/layouts/default.vue` — returns nothing (or only unrelated matches).
    - `grep -nE "bg-(green|red)|text-(green|red)" packages/web/app/pages/deputies/\[id\].vue` — returns nothing.
    - `grep -n "activeTag.value = null" packages/web/app/pages/deputies/\[id\].vue` — absent inside a page-change watcher; if present, must be inside a different context (e.g., a reset handler).
    - `grep -n "Sénat" packages/web/app/pages/about.vue` — present.
    - `grep -n "À définir" packages/web/app/pages/legal.vue` — absent (or only inside an explicit TODO comment you left intentionally).
    - `grep -n "data-testid=\"tag-filter-pill\"" packages/web/app/pages/deputies/\[id\].vue` — present on tag pill element(s).
    - `grep -n "data-testid=\"deputy-load-more\"" packages/web/app/pages/deputies/\[id\].vue` — present on the load-more button.
    - `grep -n "data-testid=\"vote-card\"" packages/web/app/pages/votes/index.vue` — present on the vote card wrapper.
    - `grep -c "\[x\]\|\[~\]" .planning/phases/17-ui-polish-bug-hunt/17-01-PUNCHLIST.md` — equal to the number of `owner: 17-02` items in the list (every one handled).
    - `pnpm --filter @poliscope/web dev` boots with no new warnings beyond baseline.
  </verify>
  <done>
    All 5 known-issue fixes landed. All 17-02 punch list items closed (fixed or explicitly deferred with reason). DA preserved. Dev server clean.
  </done>
</task>

</tasks>

<verification>
- All punch list `owner: 17-02` items are `[x]` (fixed) or `[~]` (deferred with reason).
- No regressions in existing Playwright suite (`pnpm --filter @poliscope/web test:e2e` — baseline still green; 17-04 extends, doesn't depend on this).
- Manual viewport check at 1280 / 768 / 390 for /, /debates/[id], /deputies/[id], /about, /legal.
- Grep guards: no new `shadow-*`, `bg-green*`, `bg-red*`, `text-green*`, `text-red*`.
</verification>

<success_criteria>
All polish-class bugs in the scope of plan 17-02 are gone. InterventionCard has density + differentiation without drifting from marble/bronze. DA violation on deputy profile is eliminated. Mobile nav is reachable.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-polish-bug-hunt/17-02-SUMMARY.md`
</output>
