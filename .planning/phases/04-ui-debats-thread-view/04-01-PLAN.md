---
phase: 04-ui-debats-thread-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/assets/css/main.css
  - app/components/GroupBadge.vue
  - app/components/InterventionCard.vue
  - app/components/LoadingSpinner.vue
  - app/utils/groupColors.ts
autonomous: true

must_haves:
  truths:
    - "GroupBadge displays a colored pill for each known political group"
    - "InterventionCard renders speaker name, group badge, role, content, and avatar/initials"
    - "InterventionCard handles deputy: null gracefully (ministers, president)"
    - "LoadingSpinner shows a visible loading indicator"
  artifacts:
    - path: "app/components/GroupBadge.vue"
      provides: "Political group colored badge"
      min_lines: 15
    - path: "app/components/InterventionCard.vue"
      provides: "Thread-style intervention post"
      min_lines: 40
    - path: "app/components/LoadingSpinner.vue"
      provides: "Reusable loading state"
      min_lines: 5
    - path: "app/utils/groupColors.ts"
      provides: "Group acronym to color mapping"
      exports: ["getGroupColor", "getGroupLabel"]
  key_links:
    - from: "app/components/InterventionCard.vue"
      to: "app/components/GroupBadge.vue"
      via: "auto-imported component"
      pattern: "<GroupBadge"
    - from: "app/components/GroupBadge.vue"
      to: "app/utils/groupColors.ts"
      via: "auto-imported util"
      pattern: "getGroupColor"
---

<objective>
Install @vueuse/core and build the shared UI components needed by both the debates list and thread pages.

Purpose: Create the visual building blocks (InterventionCard, GroupBadge, LoadingSpinner) and the political group color system before building the pages that consume them.
Output: 3 components, 1 util, updated CSS with group colors.
</objective>

<execution_context>
@C:/Users/Dharma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dharma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-debats-thread-view/04-RESEARCH.md
@server/api/debates/[id].get.ts
@server/db/schema.ts
@app/assets/css/main.css
@app/layouts/default.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vueuse/core + group color system</name>
  <files>
    package.json
    app/assets/css/main.css
    app/utils/groupColors.ts
  </files>
  <action>
    1. Install @vueuse/core:
       ```bash
       npm install @vueuse/core
       ```

    2. Create `app/utils/groupColors.ts` — a mapping of political group acronyms to hex colors and full labels. The actual groups in the DB are: ECO, GDR, HOR, LFI, LIOT, LR, MODEM, NI, REN, RN, SOC.

       Export two functions:
       - `getGroupColor(group: string | null): string` — returns a hex color string. Fallback to gray (#6b7280) for null/unknown.
       - `getGroupLabel(group: string | null): string` — returns the full label (e.g., "LFI" -> "La France insoumise"). Fallback to the raw string or "Non-inscrit" for null.

       Color map (use these exact values, sourced from standard French political palette):
       - LFI: #cc2443 (red)
       - GDR: #c9462c (dark orange-red)
       - SOC: #ff8080 (pink-red)
       - ECO: #00c000 (green)
       - LIOT: #ffcc00 (yellow)
       - MODEM: #ff9900 (orange)
       - REN: #ffb800 (amber) — Renaissance
       - HOR: #0099cc (teal) — Horizons
       - LR: #0066cc (blue)
       - RN: #0d378a (dark blue)
       - NI: #aaaaaa (gray) — Non-inscrits

    3. Update `app/assets/css/main.css` — keep existing @import and @theme block, just add a comment noting group colors are handled via JS util (do NOT add CSS variables for each group — they'd be unused since we use inline styles for dynamic colors).
  </action>
  <verify>
    - `npm ls @vueuse/core` shows installed version
    - `app/utils/groupColors.ts` exists and exports getGroupColor, getGroupLabel
    - `getGroupColor('LFI')` returns '#cc2443', `getGroupColor(null)` returns '#6b7280'
  </verify>
  <done>@vueuse/core installed. Group color util covers all 11 groups + null fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared components — GroupBadge, InterventionCard, LoadingSpinner</name>
  <files>
    app/components/GroupBadge.vue
    app/components/InterventionCard.vue
    app/components/LoadingSpinner.vue
  </files>
  <action>
    1. **GroupBadge.vue** — Small pill badge showing political group acronym with the group's color as background.
       - Props: `group: string | null`
       - Use `getGroupColor(group)` from auto-imported utils for inline `background-color` style
       - Use `getGroupLabel(group)` for the `title` attribute (tooltip on hover)
       - Display the acronym text (the raw `group` prop) in white, bold, text-xs
       - Render nothing (v-if) when group is null
       - Classes: inline-flex, items-center, px-2, py-0.5, rounded-full, text-xs, font-bold, text-white

    2. **InterventionCard.vue** — Twitter-thread-style post for a single intervention. This is the key visual component.
       - Props interface matching the API response shape from `/api/debates/[id]`:
         ```
         speakerName: string
         speakerRole: string | null
         content: string
         orderInDebate: number
         deputy: { fullName: string; group: string | null; photoUrl: string | null } | null
         tags: { id: number; name: string; slug: string }[]
         ```
       - Computed `displayName`: `deputy?.fullName ?? speakerName`
       - Computed `initials`: first letter of each word in displayName, max 2, uppercase
       - Layout: flex row with gap-3, left column = avatar (w-10 h-10), right column = content
       - Avatar: `<img>` if `deputy?.photoUrl` exists (rounded-full, object-cover), else a div with initials on bg-primary background
       - Header line: displayName (font-semibold text-sm), GroupBadge if deputy?.group, speakerRole in text-xs text-gray-500
       - Content: `<p>` with text-sm text-gray-800 leading-relaxed, use `v-html` is NOT allowed — use whitespace-pre-wrap for line breaks
       - Bottom border: border-b border-gray-100, py-4
       - Mobile-first: works on small screens, no fixed widths

    3. **LoadingSpinner.vue** — Simple centered loading indicator.
       - No props needed
       - A centered div with an SVG spinner animation (animate-spin, w-6 h-6, text-primary)
       - Wrap in py-8 text-center
  </action>
  <verify>
    - All 3 files exist under app/components/
    - InterventionCard uses optional chaining for deputy (deputy?.fullName, deputy?.photoUrl, deputy?.group)
    - GroupBadge uses getGroupColor for inline style
    - No `v-html` in InterventionCard
  </verify>
  <done>3 shared components built: GroupBadge (colored pill), InterventionCard (thread post with avatar/name/group/content), LoadingSpinner (animated spinner). All handle null deputy gracefully.</done>
</task>

</tasks>

<verification>
- `npm ls @vueuse/core` confirms installation
- All component files exist and have correct prop types
- InterventionCard handles deputy: null without error (optional chaining everywhere)
- GroupBadge renders nothing for null group
- Group colors cover all 11 DB values
</verification>

<success_criteria>
- @vueuse/core in package.json dependencies
- 3 components in app/components/ auto-importable by Nuxt
- 1 util in app/utils/ auto-importable by Nuxt
- InterventionCard matches API response shape from /api/debates/[id]
- All 11 political groups have assigned colors
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-debats-thread-view/04-01-SUMMARY.md`
</output>
