---
phase: 14-frontend-votes-senat-bicameral
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/app/components/ScrutinCard.vue
  - packages/web/app/components/ScrutinResultBar.vue
  - packages/web/app/components/VotePositionBadge.vue
  - packages/web/app/pages/votes/index.vue
  - packages/web/app/pages/votes/[id].vue
autonomous: true

must_haves:
  truths:
    - "Un utilisateur peut accéder à /votes et voir une liste de scrutins"
    - "La liste /votes affiche un filtre chambre (Tous/AN/Sénat) et un filtre résultat (Tous/Adopté/Rejeté)"
    - "Cliquer sur un scrutin ouvre /votes/[id] avec les détails du vote et la liste paginée des votants"
    - "La page /votes/[id] affiche la barre visuelle pour/contre/abstention et permet de filtrer par position"
    - "Une URL non-numérique comme /votes/abc retourne une 404 sans 500"
  artifacts:
    - path: "packages/web/app/components/ScrutinCard.vue"
      provides: "Card cliquable pour la liste des scrutins"
      min_lines: 40
    - path: "packages/web/app/components/ScrutinResultBar.vue"
      provides: "Barre CSS flexbox montrant les ratios pour/contre/abstention"
      min_lines: 20
    - path: "packages/web/app/components/VotePositionBadge.vue"
      provides: "Badge coloré (Pour/Contre/Abstention/Absent)"
      min_lines: 15
    - path: "packages/web/app/pages/votes/index.vue"
      provides: "Liste paginée des scrutins avec filtres chambre + résultat"
      min_lines: 80
    - path: "packages/web/app/pages/votes/[id].vue"
      provides: "Detail d'un scrutin + liste paginée des votes par acteur avec filtre position"
      min_lines: 120
  key_links:
    - from: "pages/votes/index.vue"
      to: "/api/votes"
      via: "useFetch avec query { page, chamber, result }"
    - from: "pages/votes/[id].vue"
      to: "/api/votes/[id]"
      via: "useFetch avec query { page, limit: 50, position }"
    - from: "ScrutinCard.vue"
      to: "/votes/:id"
      via: "NuxtLink :to"
---

<objective>
Créer les pages votes/index.vue et votes/[id].vue ainsi que les trois composants qui les supportent : ScrutinCard, ScrutinResultBar, VotePositionBadge.

Purpose: Ce plan délivre l'intégralité de la fonctionnalité "explorer les scrutins" (SC1 + SC2 des critères de succès de la phase 14).
Output: 2 nouvelles pages + 3 nouveaux composants — aucune modification de fichiers existants dans ce plan.
</objective>

<execution_context>
@C:/Users/Dharma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dharma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/web/app/pages/index.vue
@packages/web/app/pages/deputies/[id].vue
@packages/web/app/components/DebateCard.vue
@packages/web/app/components/GroupBadge.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScrutinCard, ScrutinResultBar, VotePositionBadge components</name>
  <files>
    packages/web/app/components/ScrutinCard.vue
    packages/web/app/components/ScrutinResultBar.vue
    packages/web/app/components/VotePositionBadge.vue
  </files>
  <action>
Créer trois composants dans packages/web/app/components/.

**ScrutinCard.vue** — modèle : DebateCard.vue (NuxtLink block, marble/bronze design).
Props: `id: number`, `title: string`, `date: string`, `chamber: string | null`, `result: string | null`, `votesFor: number | null`, `votesAgainst: number | null`, `votesAbstain: number | null`, `scrutinType: string | null`.
- `formattedDate` avec `new Intl.DateTimeFormat('fr-FR', { dateStyle: 'long', timeZone: 'Europe/Paris' })` (obligatoire — évite la mismatch SSR/client).
- Badge `result` : "Adopté" en vert (`bg-green-500/10 text-green-700`) pour `adopted`, "Rejeté" en rouge (`bg-red-500/10 text-red-700`) pour `rejected`.
- Badge chambre : identique à DebateCard (bronze/10 + text-bronze pour AN, ink/10 + text-ink pour Sénat).
- Inclure `<ScrutinResultBar>` sous le titre si `votesFor`, `votesAgainst`, `votesAbstain` sont non-nuls.
- `NuxtLink :to="'/votes/' + id"`, class `block bg-parchment rounded-xl border border-stone-border p-4 hover:border-bronze/40 transition-colors duration-200`.

**ScrutinResultBar.vue** — barre CSS flexbox, aucune librairie.
Props: `votesFor: number`, `votesAgainst: number`, `votesAbstain: number`.
- `total = (votesFor ?? 0) + (votesAgainst ?? 0) + (votesAbstain ?? 0)`.
- `pctFor`, `pctAgainst`, `pctAbstain` = `total > 0 ? (n / total * 100).toFixed(1) : 0`.
- Template : `<div class="flex h-2 rounded-full overflow-hidden gap-0.5">` avec trois divs `<div :style="{ width: pctFor + '%' }" class="bg-green-500 rounded-full" />`, `bg-red-400` pour against, `bg-stone-400` pour abstain.
- Ajouter sous la barre trois labels xs : `{{ votesFor }} pour · {{ votesAgainst }} contre · {{ votesAbstain }} abstentions`.

**VotePositionBadge.vue** — badge coloré pour une position de vote.
Props: `position: 'for' | 'against' | 'abstain' | 'absent'`.
- Map label/color : `for` → "Pour" vert (`bg-green-500/10 text-green-700`), `against` → "Contre" rouge (`bg-red-500/10 text-red-700`), `abstain` → "Abstention" (`bg-stone-200 text-ink-muted`), `absent` → "Absent" (`bg-marble-dark text-ink-muted/60`).
- Utiliser `computed` pour le label et la classe CSS. Template : `<span class="px-2 py-0.5 rounded-full text-xs font-medium" :class="colorClass">{{ label }}</span>`.
  </action>
  <verify>
Les trois fichiers existent dans packages/web/app/components/. Chaque composant compile sans erreur TypeScript (rtk tsc --noEmit depuis packages/web). ScrutinResultBar affiche bien trois segments avec pourcentages calculés.
  </verify>
  <done>
ScrutinCard.vue, ScrutinResultBar.vue, VotePositionBadge.vue existent et sont typés correctement. Aucune erreur TypeScript. Les badges résultat et chambre suivent le design system marble/bronze/ink existant.
  </done>
</task>

<task type="auto">
  <name>Task 2: Page /votes (liste) avec filtres chambre + résultat</name>
  <files>
    packages/web/app/pages/votes/index.vue
  </files>
  <action>
Créer packages/web/app/pages/votes/index.vue en suivant EXACTEMENT le pattern de packages/web/app/pages/index.vue (debates list).

**State** :
```ts
const chamber = ref<string | undefined>(undefined)
const result = ref<string | undefined>(undefined)
const page = ref(1)
const allScrutins = ref<any[]>([])
const sentinel = useTemplateRef('sentinel')
```

**Fetch** :
```ts
const { data, status } = await useFetch('/api/votes', {
  query: { page, limit: 20, chamber, result },
  lazy: true,
  watch: [page, chamber, result],
})
```

**Titre dynamique** : computed sur `chamber` : "Scrutins de l'Assemblée nationale" / "Scrutins du Sénat" / "Scrutins parlementaires".

**Reset sur filtre** : une seule fonction `selectChamber(val)` et `selectResult(val)` — chacune fait `page.value = 1; allScrutins.value = []` avant de changer le ref.

**Watcher data** (pattern deputies/index.vue — évite les doublons pitfall 1) :
```ts
watch(data, (newData) => {
  if (!newData?.data) return
  if (page.value === 1) { allScrutins.value = newData.data }
  else { allScrutins.value.push(...newData.data) }
}, { immediate: true })
```

**Infinite scroll** : IntersectionObserver sur sentinel avec rootMargin: '200px', incrémente page si `!pending && hasMore`.

**Template** :
- `<h1 font-heading>` avec pageTitle computed
- Pills chambre (Tous / AN / Sénat) — classes activeClass/inactiveClass identiques à index.vue
- Pills résultat (Tous / Adopté / Rejeté) — même classes, second groupe de pills
- Grid `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` avec `<ScrutinCard v-for v-bind="scrutin" />`
- Empty state, LoadingSpinner, sentinel, "Tous les scrutins chargés"

**SEO** : `useSeoMeta({ title: 'Votes', description: 'Les scrutins parlementaires' })`.
  </action>
  <verify>
Le fichier existe. `rtk tsc --noEmit` depuis packages/web passe sans erreur. La page est accessible à /votes (naviguer manuellement ou vérifier que Nuxt détecte la route via `ls packages/web/app/pages/votes/`).
  </verify>
  <done>
/votes affiche la liste des scrutins avec filtres chambre et résultat. Le changement de filtre remet page à 1 et vide allScrutins. L'infinite scroll charge la page suivante quand le sentinel entre dans le viewport.
  </done>
</task>

<task type="auto">
  <name>Task 3: Page /votes/[id] (détail scrutin + votes paginés par position)</name>
  <files>
    packages/web/app/pages/votes/[id].vue
  </files>
  <action>
Créer packages/web/app/pages/votes/[id].vue.

**Validation route** (obligatoire — évite 500 sur slugs non-numériques) :
```ts
definePageMeta({
  validate: (route) => /^\d+$/.test(String(route.params.id)),
})
```

**State** :
```ts
const route = useRoute()
const position = ref<string | null>(null)
const page = ref(1)
const allVotes = ref<any[]>([])
const sentinel = useTemplateRef('sentinel')
```

**Fetch** :
```ts
const { data, status, error } = await useFetch(`/api/votes/${route.params.id}`, {
  query: { page, limit: 50, position },
  watch: [page, position],
})
```

**Reset votes sur changement position** (pattern anti-pitfall 1) :
```ts
watch(position, () => { allVotes.value = []; page.value = 1 })
watch(data, (newData) => {
  if (!newData?.votes?.data) return
  if (page.value === 1) { allVotes.value = newData.votes.data }
  else { allVotes.value.push(...newData.votes.data) }
}, { immediate: true })
```

**SEO** : `useSeoMeta({ title: () => data.value?.scrutin?.title ?? 'Scrutin' })`.

**Template** :
1. Loading state : `<LoadingSpinner v-if="status === 'pending' && !data" />`
2. Error state : 404 → "Scrutin introuvable.", autre → "Une erreur est survenue."
3. Contenu :
   - `&larr; Retour aux scrutins` → NuxtLink to="/votes"
   - Header scrutin : titre (font-heading h1), date formatée (`Intl.DateTimeFormat fr-FR timeZone Europe/Paris`), badges chambre + résultat (même logique que ScrutinCard)
   - `<ScrutinResultBar>` avec les compteurs du scrutin (si non-nuls)
   - Lien source officiel `<a :href="data.scrutin.sourceUrl" target="_blank">` si sourceUrl non-nul
   - Divider puis titre "Votes des parlementaires"
   - Pills filtre position : Tous / Pour / Contre / Abstention / Absent — valeurs `null/'for'/'against'/'abstain'/'absent'`
   - Liste paginée : chaque vote = ligne avec photo (img ou initiales fallback), lien `/deputies/:actorId`, `<VotePositionBadge :position="vote.position" />`, nom + badge groupe `<GroupBadge :group="vote.actorGroup" />`
   - LoadingSpinner pendant fetch, sentinel pour infinite scroll, "Tous les votes chargés"
   - hasMore computed sur `data.value?.votes?.pagination`

**Ligne de vote** HTML : `<div class="flex items-center gap-3 py-2 border-b border-stone-border/50">` avec photo ronde 8x8, nom en lien bronze, GroupBadge, VotePositionBadge à droite.
  </action>
  <verify>
Le fichier existe. `rtk tsc --noEmit` passe. `ls packages/web/app/pages/votes/` montre `index.vue` et `[id].vue`. Navigation vers /votes/abc → Nuxt retourne 404 (validate rejette). Navigation vers /votes/1 → page s'affiche avec données scrutin.
  </verify>
  <done>
/votes/[id] affiche le scrutin, la barre visuelle, les filtres de position, et la liste paginée des votes. Filtre position remet allVotes à vide et recommence depuis page 1. URL invalide → 404.
  </done>
</task>

</tasks>

<verification>
Après exécution :
1. `rtk tsc --noEmit` depuis packages/web — zéro erreur TypeScript
2. `ls packages/web/app/components/Scrutin*.vue packages/web/app/components/VotePositionBadge.vue` — 3 fichiers présents
3. `ls packages/web/app/pages/votes/` — index.vue + [id].vue présents
4. Naviguer manuellement vers /votes → liste charge, filtres chambre et résultat fonctionnent
5. Naviguer vers /votes/1 → détail s'affiche, barre résultat visible, filtres position fonctionnent
6. Naviguer vers /votes/abc → 404 (pas 500)
</verification>

<success_criteria>
- /votes affiche la liste des scrutins avec filtres chambre (AN/Sénat/Tous) et résultat (Adopté/Rejeté/Tous)
- L'infinite scroll charge les pages suivantes sans doublons au changement de filtre
- /votes/[id] affiche le détail du scrutin, la barre visuelle for/against/abstain, et les votes paginés
- Le filtre par position (Pour/Contre/Abstention/Absent) sur /votes/[id] fonctionne sans doublons
- Les URLs non-numériques retournent 404
- Design cohérent avec le reste de l'app (marble/bronze/ink, Cinzel headings, pas d'ombres)
</success_criteria>

<output>
After completion, create `.planning/phases/14-frontend-votes-senat-bicameral/14-01-SUMMARY.md`
</output>
