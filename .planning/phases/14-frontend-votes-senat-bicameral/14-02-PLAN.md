---
phase: 14-frontend-votes-senat-bicameral
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/app/layouts/default.vue
  - packages/web/app/pages/deputies/index.vue
  - packages/web/app/pages/deputies/[id].vue
  - packages/web/app/components/SearchResultCard.vue
  - packages/web/app/pages/search.vue
autonomous: true

must_haves:
  truths:
    - "Un lien 'Votes' est visible dans la nav principale et pointe vers /votes"
    - "La page /deputies affiche un filtre chambre (Tous/AN/Sénat) — les sénateurs apparaissent quand 'Sénat' est sélectionné"
    - "Le profil d'un sénateur sur /deputies/[id] affiche un badge indiquant 'Sénat'"
    - "La recherche retourne des résultats de type 'scrutin' avec un badge distinctif et un lien vers /votes/:id"
    - "Le filtre par type (Interventions/Votes/Tous) est disponible sur la page /search"
  artifacts:
    - path: "packages/web/app/layouts/default.vue"
      provides: "Nav avec lien Votes + renommage 'Parlementaires'"
      contains: "to=\"/votes\""
    - path: "packages/web/app/pages/deputies/index.vue"
      provides: "Liste des parlementaires avec filtre chambre pills"
      contains: "selectChamber"
    - path: "packages/web/app/components/SearchResultCard.vue"
      provides: "Rendu conditionnel selon type intervention/scrutin"
      contains: "type === 'scrutin'"
    - path: "packages/web/app/pages/search.vue"
      provides: "Filtre type + passage du type à SearchResultCard"
      contains: "typeFilter"
  key_links:
    - from: "search.vue"
      to: "SearchResultCard.vue"
      via: "prop type + context_id pour lien /votes/:id"
    - from: "deputies/index.vue"
      to: "/api/deputies"
      via: "query { chamber } — déjà supporté par l'API"
---

<objective>
Relier les nouvelles fonctionnalités votes/sénat au reste de l'interface : navigation globale, liste des parlementaires bicamérale, profil sénateur avec badge chambre, résultats de recherche cross-type avec rendu scrutin.

Purpose: Ce plan délivre la navigation bicamérale (SC4) et la recherche cross-type avec badges (SC5), complétant la phase 14.
Output: 3 fichiers mis à jour (layout, deputies/index, search) + 1 composant mis à jour (SearchResultCard) + 1 ajout mineur (deputies/[id]).
</objective>

<execution_context>
@C:/Users/Dharma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dharma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/web/app/layouts/default.vue
@packages/web/app/pages/deputies/index.vue
@packages/web/app/pages/deputies/[id].vue
@packages/web/app/components/SearchResultCard.vue
@packages/web/app/pages/search.vue
@packages/web/app/pages/index.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav + deputies list chambre filter + senator badge</name>
  <files>
    packages/web/app/layouts/default.vue
    packages/web/app/pages/deputies/index.vue
    packages/web/app/pages/deputies/[id].vue
  </files>
  <action>
**1. packages/web/app/layouts/default.vue** — ajouter le lien Votes et renommer Députés en Parlementaires.

Dans le `<nav>`, insérer entre le lien Débats et le lien Parlementaires :
```html
<NuxtLink to="/votes" active-class="text-bronze" class="hover:text-bronze transition-colors duration-200">Votes</NuxtLink>
```
Changer le texte du lien `/deputies` de "Députés" à "Parlementaires".

**2. packages/web/app/pages/deputies/index.vue** — ajouter filtre chambre pills.

Ajouter le state `chamber` et la fonction `selectChamber` exactement comme dans index.vue (debates) :
```ts
const chamber = ref<string | undefined>(undefined)

function selectChamber(newVal: string | undefined) {
  page.value = 1
  allDeputies.value = []
  chamber.value = newVal
}
```

Ajouter `chamber` dans la query du useFetch :
```ts
const { data, status } = await useFetch('/api/deputies', {
  query: { page, limit: 20, search, group, chamber },
  lazy: true,
  watch: [page, search, group, chamber],
})
```

Ajouter `chamber` dans le watch reset (aux côtés de search et group) :
```ts
watch([search, group, chamber], () => {
  allDeputies.value = []
  page.value = 1
})
```

Mettre à jour le titre dynamique avec computed :
```ts
const pageTitle = computed(() => {
  if (chamber.value === 'AN') return "Députés de l'Assemblée nationale"
  if (chamber.value === 'Senat') return 'Sénateurs'
  return 'Parlementaires'
})
```
Remplacer le `<h1>` statique par `{{ pageTitle }}`. Mettre à jour `useSeoMeta` : `title: 'Parlementaires'`, `description: 'Liste des parlementaires français'`.

Ajouter les pills chambre dans le template, AVANT le bloc search/group existant :
```html
<div class="flex gap-2 mb-4">
  <button :class="[!chamber ? activeClass : inactiveClass]" @click="selectChamber(undefined)">Tous</button>
  <button :class="[chamber === 'AN' ? activeClass : inactiveClass]" @click="selectChamber('AN')">Assemblée nationale</button>
  <button :class="[chamber === 'Senat' ? activeClass : inactiveClass]" @click="selectChamber('Senat')">Sénat</button>
</div>
```
Définir les constantes activeClass/inactiveClass identiques à index.vue (debates) :
```ts
const activeClass = 'bg-bronze text-white rounded-full px-4 py-1.5 text-sm font-medium'
const inactiveClass = 'bg-marble-dark text-ink-muted rounded-full px-4 py-1.5 text-sm font-medium hover:bg-marble-dark/80'
```
Mettre à jour le texte du placeholder input : "Rechercher un parlementaire...". Mettre à jour l'empty state : "Aucun parlementaire trouvé". Mettre à jour "Tous les députés chargés" → "Tous les parlementaires chargés".

**3. packages/web/app/pages/deputies/[id].vue** — ajouter badge chambre dans le header du profil.

Après le `<GroupBadge>` existant dans la section header, ajouter conditionnellement un badge chambre :
```html
<span
  v-if="data.deputy?.chamber === 'AN'"
  class="text-xs bg-bronze/10 text-bronze px-2 py-0.5 rounded-full font-medium"
>AN</span>
<span
  v-else-if="data.deputy?.chamber === 'Senat'"
  class="text-xs bg-ink/10 text-ink px-2 py-0.5 rounded-full font-medium"
>Sénat</span>
```
Mettre à jour le lien "Retour" : changer "← Retour aux députés" → "← Retour aux parlementaires".
  </action>
  <verify>
`rtk tsc --noEmit` depuis packages/web passe sans erreur. Vérifier que default.vue contient "Votes" et "Parlementaires" dans la nav. Vérifier que deputies/index.vue contient `selectChamber` et les pills chambre. Vérifier que deputies/[id].vue contient `data.deputy?.chamber`.
  </verify>
  <done>
La nav affiche Débats / Votes / Parlementaires / Recherche. La page /deputies affiche les pills chambre — sélectionner "Sénat" filtre les sénateurs. Le profil d'un sénateur affiche le badge "Sénat" dans son header.
  </done>
</task>

<task type="auto">
  <name>Task 2: SearchResultCard scrutin variant + search.vue type filter</name>
  <files>
    packages/web/app/components/SearchResultCard.vue
    packages/web/app/pages/search.vue
  </files>
  <action>
**1. packages/web/app/components/SearchResultCard.vue** — ajouter le variant scrutin.

Le composant doit accepter un discriminant `type` et rendre deux layouts différents.

Remplacer les props existantes par une union typée. Garder toutes les props intervention existantes et ajouter les props scrutin optionnelles :
```ts
const props = defineProps<{
  // Discriminant
  type?: 'intervention' | 'scrutin'
  // Props partagées
  highlight: string
  // Props intervention (existantes — toutes rendues optionnelles)
  speakerName?: string
  speakerRole?: string | null
  debate?: { id: number; title: string; date: string; sessionType: string | null }
  deputy?: { id: number; fullName: string | null; group: string | null; photoUrl: string | null } | null
  tags?: Array<{ name: string; slug: string }>
  orderInDebate?: number
  // Props scrutin (nouvelles)
  scrutinId?: number   // context_id from API = scrutin.id
  title?: string
  date?: string
  result?: string | null
  votesFor?: number | null
  votesAgainst?: number | null
  votesAbstain?: number | null
  chamber?: string | null
}>()
```

Adapter `formattedDate` pour être compatible avec les deux types (utiliser `props.debate?.date ?? props.date`).

Dans le template, utiliser `v-if="props.type !== 'scrutin'"` pour l'ancien rendu intervention (blocs existants inchangés) et `v-else` pour le rendu scrutin :

Rendu scrutin (`v-else`) :
```html
<div data-testid="search-result-card-scrutin" class="bg-parchment border border-stone-border rounded-lg overflow-hidden">
  <!-- Header avec badge "Vote" distinctif -->
  <div class="px-4 pt-3 pb-2">
    <div class="flex items-start justify-between gap-3">
      <NuxtLink
        :to="`/votes/${scrutinId}`"
        class="text-sm font-semibold font-heading text-ink hover:text-bronze transition-colors duration-200 line-clamp-2"
      >
        <!-- Contenu highlight via v-html (FTS mark tags) -->
        <span v-html="highlight" />
      </NuxtLink>
      <span class="text-xs text-ink-muted whitespace-nowrap flex-shrink-0">{{ formattedDate }}</span>
    </div>
    <div class="flex items-center gap-2 mt-1">
      <!-- Badge type distinctif -->
      <span class="text-xs bg-bronze/10 text-bronze px-2 py-0.5 rounded-full font-medium uppercase tracking-wider">Vote</span>
      <!-- Badge chambre -->
      <span v-if="chamber === 'AN'" class="text-xs bg-bronze/10 text-bronze px-2 py-0.5 rounded-full font-medium">AN</span>
      <span v-else-if="chamber === 'Senat'" class="text-xs bg-ink/10 text-ink px-2 py-0.5 rounded-full font-medium">Sénat</span>
      <!-- Badge résultat -->
      <span v-if="result === 'adopted'" class="text-xs bg-green-500/10 text-green-700 px-2 py-0.5 rounded-full font-medium">Adopté</span>
      <span v-else-if="result === 'rejected'" class="text-xs bg-red-500/10 text-red-700 px-2 py-0.5 rounded-full font-medium">Rejeté</span>
    </div>
  </div>
  <!-- Barre résultat si données disponibles -->
  <div v-if="votesFor != null && votesAgainst != null && votesAbstain != null" class="px-4 pb-3">
    <ScrutinResultBar :votes-for="votesFor" :votes-against="votesAgainst" :votes-abstain="votesAbstain" />
  </div>
</div>
```

Garder le `emit('filter-tag', slug)` existant — il reste utilisé par le rendu intervention.

**2. packages/web/app/pages/search.vue** — ajouter le filtre type + passer les bonnes props.

Ajouter le state type filter :
```ts
const typeFilter = ref<string>((route.query.type as string) ?? '')
```

Ajouter `typeFilter` dans le watch reset (aux côtés de q, activeTag, deputyId) et dans le sync URL router.replace.

Ajouter dans la query du useFetch :
```ts
...(typeFilter.value ? { type: typeFilter.value } : {}),
```

Ajouter dans le template des pills type (après le compteur de résultats, avant la liste) :
```html
<div class="flex gap-2 mb-4">
  <button :class="[!typeFilter ? activeClass : inactiveClass]" @click="typeFilter = ''">Tous</button>
  <button :class="[typeFilter === 'intervention' ? activeClass : inactiveClass]" @click="typeFilter = 'intervention'">Interventions</button>
  <button :class="[typeFilter === 'scrutin' ? activeClass : inactiveClass]" @click="typeFilter = 'scrutin'">Votes</button>
</div>
```
Définir les constantes activeClass/inactiveClass (identiques aux autres pages).

Mettre à jour l'itération des résultats pour passer le type et les bonnes props selon le type :
```html
<template v-for="result in allResults" :key="result.id">
  <!-- Intervention -->
  <SearchResultCard
    v-if="result.type !== 'scrutin'"
    type="intervention"
    :speaker-name="result.speakerName"
    :speaker-role="result.speakerRole"
    :highlight="result.highlight"
    :debate="result.debate"
    :deputy="result.deputy"
    :tags="result.tags ?? []"
    :order-in-debate="result.orderInDebate"
    @filter-tag="handleFilterTag"
  />
  <!-- Scrutin -->
  <SearchResultCard
    v-else
    type="scrutin"
    :scrutin-id="result.id"
    :highlight="result.highlight"
    :title="result.title"
    :date="result.date"
    :result="result.result"
    :votes-for="result.votesFor"
    :votes-against="result.votesAgainst"
    :votes-abstain="result.votesAbstain"
    :chamber="result.chamber"
  />
</template>
```

Note : la prop `context_id` de l'API pour les scrutins est `s.id` (même valeur que `row.id` dans la réponse normalisée — cf. search.get.ts ligne 256-267, le type scrutin retourne `id: row.id`). Utiliser directement `result.id` comme `scrutinId`.
  </action>
  <verify>
`rtk tsc --noEmit` depuis packages/web passe sans erreur. Dans la page /search, taper un terme ambigu (ex: "loi") → les résultats scrutin affichent le badge "Vote" bronze. Cliquer sur un résultat scrutin navigue vers /votes/:id. Le filtre type "Votes" affiche uniquement les scrutins, "Interventions" uniquement les interventions.
  </verify>
  <done>
SearchResultCard rend le layout scrutin (badge "Vote", barre résultat, lien /votes/:id) quand `type === 'scrutin'`. search.vue passe le bon type et les bonnes props pour chaque résultat. Les pills type permettent de filtrer par type de résultat.
  </done>
</task>

</tasks>

<verification>
Après exécution :
1. `rtk tsc --noEmit` depuis packages/web — zéro erreur TypeScript
2. La nav contient exactement 4 liens : Débats, Votes, Parlementaires, Recherche
3. /deputies → pills chambre visibles, sélectionner "Sénat" filtre les sénateurs
4. /deputies/:id pour un sénateur → badge "Sénat" visible dans le header
5. /search?q=loi → résultats scrutin affichent badge "Vote" (bronze uppercase) et lien vers /votes/:id
6. Filtre type "Votes" sur /search → uniquement des scrutins retournés
</verification>

<success_criteria>
- Lien "Votes" présent dans la nav, actif sur les pages /votes/* (active-class="text-bronze")
- /deputies affiche filtre chambre pills — sénateurs visibles avec filtre "Sénat"
- Profil sénateur sur /deputies/[id] affiche badge "Sénat" à côté du badge groupe
- Résultats scrutin dans /search ont un badge "Vote" distinctif bronze + lien vers /votes/:id
- Filtre type (Tous/Interventions/Votes) fonctionne sur /search
- Aucune régression sur les fonctionnalités intervention existantes de /search
</success_criteria>

<output>
After completion, create `.planning/phases/14-frontend-votes-senat-bicameral/14-02-SUMMARY.md`
</output>
