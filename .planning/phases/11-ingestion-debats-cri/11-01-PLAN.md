---
phase: 11-ingestion-debats-cri
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ingestion/scripts/ingest_debates.py
autonomous: true

must_haves:
  truths:
    - "ingest_debates.py queries FROM actors WHERE chamber = 'AN' (not FROM deputies)"
    - "Intervention records use actor_id key (not deputy_id)"
    - "AN actor matching prepends 'PA' prefix to href fiche ID before lookup"
    - "Debates are upserted with chamber='AN' column"
    - "Interventions are inserted with chamber='AN' column"
    - "Re-running the script on same data produces no duplicates (idempotent)"
  artifacts:
    - path: "packages/ingestion/scripts/ingest_debates.py"
      provides: "Refactored AN CRI pipeline using actors table"
      contains: "FROM actors WHERE chamber"
  key_links:
    - from: "packages/ingestion/scripts/ingest_debates.py"
      to: "actors table"
      via: "load_actor_cache() query"
      pattern: "FROM actors WHERE chamber = 'AN'"
    - from: "packages/ingestion/scripts/ingest_debates.py"
      to: "debates table"
      via: "upsert_debate() with chamber column"
      pattern: "chamber.*AN"
---

<objective>
Refactor the existing `ingest_debates.py` AN CRI pipeline to use the renamed `actors` table (was `deputies`) and include `chamber` column on debates and interventions.

Purpose: The script currently references the old `deputies` table (renamed to `actors` in migration 0001) and missing `chamber` column (added in migration 0002). Without this refactoring, the pipeline will fail with `UndefinedTable` / `UndefinedColumn` errors.

Output: A working `ingest_debates.py` that ingests AN CRI sessions using the current schema.
</objective>

<execution_context>
@C:/Users/Dharma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dharma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-ingestion-debats-cri/11-RESEARCH.md
@packages/ingestion/scripts/ingest_debates.py
@packages/ingestion/scripts/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ingest_debates.py — deputy to actor migration</name>
  <files>packages/ingestion/scripts/ingest_debates.py</files>
  <action>
  Apply ALL of these changes to `ingest_debates.py`:

  1. **DEBATE_COLUMNS**: Add `"chamber"` to the list.

  2. **INTERVENTION_COLUMNS**: Rename `"deputy_id"` to `"actor_id"`. Add `"chamber"` to the list.

  3. **Rename `load_deputy_cache()` to `load_actor_cache()`**:
     - Change SQL query from `SELECT id, official_id, full_name FROM deputies` to `SELECT id, official_id, full_name FROM actors WHERE chamber = 'AN'`
     - Update logger message from "Deputy cache" to "Actor cache"

  4. **Rename `match_deputy()` to `match_actor()`**:
     - Fix AN ID matching: change `if an_id and an_id in by_official_id` to `pa_id = "PA" + an_id` then `if pa_id in by_official_id: return by_official_id[pa_id]`. This is CRITICAL — actors.official_id has PA prefix (e.g. "PA795746") but href gives just "795746".

  5. **`build_debate_record()`**: Add `"chamber": "AN"` to the returned dict.

  6. **`build_intervention_records()`**:
     - Rename internal variable `deputy_id` to `actor_id`
     - Call `match_actor()` instead of `match_deputy()`
     - Change returned dict key from `"deputy_id"` to `"actor_id"`
     - Add `"chamber": "AN"` to each returned dict
     - Rename stats keys: `deputy_matches` to `actor_matches`, `deputy_unmatched` to `actor_unmatched`

  7. **`ingest_debates()` function**:
     - Rename `load_deputy_cache` call to `load_actor_cache`
     - Rename stats keys: `deputy_matches` to `actor_matches`, `deputy_unmatched` to `actor_unmatched`

  8. **`main()` CLI summary**: Update logger messages from "Deputy" to "Actor".

  9. **Docstrings**: Update module docstring and function docstrings to say "actor" instead of "deputy".

  DO NOT change the parsing logic (_parse_speaker, parse_cri_xml, etc.) — that is correct.
  DO NOT change the download/extraction logic — that is correct.
  DO NOT change normalize_name() — it will be reused by the Senat pipeline.
  </action>
  <verify>
  Run: `cd packages/ingestion/scripts && python -c "import ingest_debates; print('Import OK')"`
  Grep for "deputy" in the file to confirm no stale references: `grep -i "deputy\|deputies" packages/ingestion/scripts/ingest_debates.py` should return ZERO matches (except possibly in comments about the migration).
  Grep for "chamber" to confirm it's present: `grep "chamber" packages/ingestion/scripts/ingest_debates.py` should show multiple matches.
  Grep for "PA" prefix: `grep "PA.*an_id\|PA.*+.*an_id" packages/ingestion/scripts/ingest_debates.py` should show the PA prefix logic.
  </verify>
  <done>
  ingest_debates.py imports without error, uses actors table with chamber='AN' filter, includes chamber in both DEBATE_COLUMNS and INTERVENTION_COLUMNS, prepends PA prefix for official_id matching, and has zero references to the old deputies table.
  </done>
</task>

</tasks>

<verification>
- `python -c "import ingest_debates"` succeeds
- Zero grep hits for `deputy_id` or `FROM deputies` in the refactored file
- `chamber` appears in both DEBATE_COLUMNS and INTERVENTION_COLUMNS
- PA prefix logic present in match_actor()
</verification>

<success_criteria>
ingest_debates.py is fully refactored to use the actors table, includes chamber columns, and the PA prefix fix ensures AN actors will be matched by official_id during ingestion.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ingestion-debats-cri/11-01-SUMMARY.md`
</output>
