---
phase: 11-ingestion-debats-cri
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/server/api/debates/index.get.ts
  - packages/web/app/pages/index.vue
  - packages/web/app/components/DebateCard.vue
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "GET /api/debates?chamber=AN retourne uniquement les debats AN"
    - "GET /api/debates?chamber=Senat retourne uniquement les debats Senat"
    - "GET /api/debates (sans param) retourne tous les debats"
    - "La page index.vue affiche un selecteur chambre (Tous / AN / Senat)"
    - "Le titre de la page reflette le filtre actif"
    - "DebateCard affiche un badge chambre (AN ou Senat)"
  artifacts:
    - path: "packages/web/server/api/debates/index.get.ts"
      provides: "Chamber filter via ?chamber= query param"
      contains: "eq(debates.chamber"
    - path: "packages/web/app/pages/index.vue"
      provides: "Chamber selector UI + dynamic title"
      contains: "chamber"
    - path: "packages/web/app/components/DebateCard.vue"
      provides: "Chamber badge on each card"
      contains: "chamber"
  key_links:
    - from: "packages/web/app/pages/index.vue"
      to: "/api/debates"
      via: "useFetch query param chamber"
      pattern: "chamber"
    - from: "packages/web/server/api/debates/index.get.ts"
      to: "debates table"
      via: "eq(debates.chamber, chamber)"
      pattern: "eq.*debates\\.chamber"
---

<objective>
Add chamber filter to the debates API endpoint and debates list UI.

Purpose: Success criterion #1 requires filtering debates by chamber (AN/Senat). The API currently returns all debates without filtering, and the UI is hardcoded to "Debats de l'Assemblee nationale" with no chamber selector or badge.

Output: API ?chamber= param, UI chamber selector tabs, DebateCard chamber badge.
</objective>

<execution_context>
@C:/Users/Dharma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dharma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/shared/src/schema.ts
@packages/web/server/api/deputies/index.get.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chamber filter to /api/debates endpoint</name>
  <files>packages/web/server/api/debates/index.get.ts</files>
  <action>
Refactor the debates index API to support an optional `?chamber=AN` or `?chamber=Senat` query parameter.

Follow the exact same pattern used in `deputies/index.get.ts`:
1. Import `eq, and` from `drizzle-orm` and `type { SQL }`
2. Read `chamber` from `getQuery(event)` as `string | undefined`
3. Build a `conditions: SQL[]` array
4. If `chamber` is provided and equals `'AN'` or `'Senat'`, push `eq(debates.chamber, chamber)` to conditions
5. Add `.where(conditions.length > 0 ? and(...conditions) : undefined)` to the query
6. Also select `chamber: debates.chamber` in the select clause so the frontend can display it

The `debates` table already has a `chamber` column (text, default 'AN') from schema.ts. No migration needed.

Do NOT change the pagination logic, window function, or error handling — only add the filter.
  </action>
  <verify>
The file compiles without error: `cd C:/Users/Dharma/Documents/Pro/Perso/Apps/Poliscope && npx nuxi typecheck 2>&1 | head -20` (or check just the file for syntax).

Manual test (after Senat data exists): `curl "http://localhost:3000/api/debates?chamber=AN&limit=2"` returns only AN debates, `curl "http://localhost:3000/api/debates?chamber=Senat&limit=2"` returns only Senat debates, `curl "http://localhost:3000/api/debates?limit=2"` returns mixed.
  </verify>
  <done>API endpoint accepts optional ?chamber param, filters via WHERE clause, returns chamber field in response objects.</done>
</task>

<task type="auto">
  <name>Task 2: Add chamber selector and badge to debates list UI</name>
  <files>packages/web/app/pages/index.vue, packages/web/app/components/DebateCard.vue</files>
  <action>
**DebateCard.vue changes:**
1. Add `chamber` prop: `chamber: string | null` to defineProps
2. Display a small badge next to sessionType showing the chamber. Use the design system colors:
   - `AN` badge: `bg-bronze/10 text-bronze` with text "AN"
   - `Senat` badge: `bg-ink/10 text-ink` with text "Senat"
3. Place the badge in the bottom-left flex area, before or alongside the sessionType span

**index.vue changes:**
1. Add a reactive `chamber` ref: `const chamber = ref<string | undefined>(undefined)` — undefined means "all chambers"
2. Pass `chamber` into the useFetch query: `query: { page, limit: 20, chamber }`
3. Replace the hardcoded title with a computed:
   - chamber === 'AN' -> "Debats de l'Assemblee nationale"
   - chamber === 'Senat' -> "Debats du Senat"
   - undefined -> "Debats parlementaires"
4. Add 3 filter buttons/tabs above the grid (below the title):
   ```html
   <div class="flex gap-2 mb-4">
     <button @click="selectChamber(undefined)" :class="[!chamber ? activeClass : inactiveClass]">Tous</button>
     <button @click="selectChamber('AN')" :class="[chamber === 'AN' ? activeClass : inactiveClass]">Assemblee nationale</button>
     <button @click="selectChamber('Senat')" :class="[chamber === 'Senat' ? activeClass : inactiveClass]">Senat</button>
   </div>
   ```
   Style with marble/bronze design system:
   - Active: `bg-bronze text-white rounded-full px-4 py-1.5 text-sm font-medium`
   - Inactive: `bg-marble-dark text-ink-muted rounded-full px-4 py-1.5 text-sm font-medium hover:bg-marble-dark/80`
5. The `selectChamber` function must reset pagination: set `page.value = 1`, clear `allDebates.value = []`, then set `chamber.value = newVal`. This ensures infinite scroll restarts from page 1 with the new filter.
6. Watch `chamber` in the useFetch watch array alongside `page`: `watch: [page, chamber]`
7. Update the `useSeoMeta` description to be generic: "Les derniers debats parlementaires"
8. Pass `chamber` prop from DebateCard: `:chamber="debate.chamber"` (the API now returns it from Task 1)
  </action>
  <verify>
Verify the files compile: `cd C:/Users/Dharma/Documents/Pro/Perso/Apps/Poliscope && npx nuxi typecheck 2>&1 | head -20`.

Check DebateCard.vue has `chamber` in defineProps. Check index.vue has the 3 filter buttons and dynamic title. Grep for hardcoded "Assemblee nationale" in h1 — should NOT appear as a static string.
  </verify>
  <done>
- Page title changes based on active filter
- 3 tab buttons (Tous / AN / Senat) are rendered
- Selecting a tab resets pagination and re-fetches with ?chamber= param
- DebateCard shows a chamber badge (AN or Senat)
- No hardcoded "Assemblee nationale" in the h1
  </done>
</task>

</tasks>

<verification>
1. `grep -n "chamber" packages/web/server/api/debates/index.get.ts` — must show eq + where usage
2. `grep -n "chamber" packages/web/app/pages/index.vue` — must show ref, filter buttons, query param
3. `grep -n "chamber" packages/web/app/components/DebateCard.vue` — must show prop + badge
4. No hardcoded "Debats de l'Assemblee nationale" in the h1 of index.vue
</verification>

<success_criteria>
- API /api/debates accepts ?chamber=AN|Senat and filters correctly
- API /api/debates without chamber returns all debates
- API response includes chamber field on each debate
- UI shows 3 filter tabs (Tous / AN / Senat)
- Page title is dynamic based on active filter
- DebateCard displays chamber badge
- Infinite scroll resets when changing filter
</success_criteria>

<output>
After completion, create `.planning/phases/11-ingestion-debats-cri/11-03-SUMMARY.md`
</output>
