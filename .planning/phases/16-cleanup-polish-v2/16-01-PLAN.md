---
phase: 16-cleanup-polish-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ingestion/scripts/run_all.py
  - packages/web/server/api/deputies/index.get.ts
  - packages/web/server/api/__sitemap__/urls.ts
  - packages/web/app/pages/deputies/[id].vue
  - packages/web/app/components/DeputyCard.vue
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "run_all.py pipeline executes without referencing non-existent deputies table"
    - "Deputy profile page displays vote position breakdown (pour/contre/abstention/absent)"
    - "Deputy cards in the grid show an AN or Senat chamber badge"
    - "Sitemap includes /votes/:id URLs for all scrutins"
  artifacts:
    - path: "packages/ingestion/scripts/run_all.py"
      provides: "Pipeline without legacy Step 6"
      contains: "Step 6 removed"
    - path: "packages/web/app/pages/deputies/[id].vue"
      provides: "voteStats rendering section"
      contains: "voteStats"
    - path: "packages/web/app/components/DeputyCard.vue"
      provides: "Chamber badge display"
      contains: "chamber"
    - path: "packages/web/server/api/__sitemap__/urls.ts"
      provides: "Scrutin URLs in sitemap"
      contains: "scrutins"
    - path: "packages/web/server/api/deputies/index.get.ts"
      provides: "chamber field in deputies list API"
      contains: "actors.chamber"
  key_links:
    - from: "packages/web/server/api/deputies/index.get.ts"
      to: "packages/web/app/components/DeputyCard.vue"
      via: "v-bind spread passes chamber prop automatically"
      pattern: "chamber.*actors\\.chamber"
    - from: "packages/web/server/api/deputies/[id].get.ts"
      to: "packages/web/app/pages/deputies/[id].vue"
      via: "data.voteStats consumed by template"
      pattern: "data\\.voteStats"
    - from: "packages/web/server/api/__sitemap__/urls.ts"
      to: "packages/web/app/pages/votes/[id].vue"
      via: "sitemap /votes/:id matches votes page route"
      pattern: "/votes/\\$\\{.*id\\}"
---

<objective>
Close 4 tech debt items from v2.0 milestone audit: remove legacy ingest_deputies.py from pipeline, render voteStats on deputy profile, add chamber badge to DeputyCard, add scrutins to sitemap.

Purpose: Finalize Milestone 2 gap closure — all 4 items are independent integration gaps found during audit.
Output: 5 files modified, 0 new files, 0 new dependencies.
</objective>

<execution_context>
@C:/Users/Dharma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dharma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cleanup-polish-v2/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend fixes — remove legacy Step 6 + add chamber to index API + add scrutins to sitemap</name>
  <files>
    packages/ingestion/scripts/run_all.py
    packages/web/server/api/deputies/index.get.ts
    packages/web/server/api/__sitemap__/urls.ts
  </files>
  <action>
**1a. Remove Step 6 from run_all.py:**
- Remove the `--skip-deputies` argparse flag entirely (the `add_argument('--skip-deputies', ...)` line)
- Remove the Step 6 block that runs `ingest_deputies.py` (the `if not args.skip_deputies: ...` block, ~lines 159-164)
- Add a comment where Step 6 was: `# Step 6 REMOVED — ingest_deputies.py targeted non-existent 'deputies' table. Use ingest_actors_an.py / ingest_actors_senat.py instead.`
- If there's a `steps` list or plan logger that references Step 6, update/remove that entry too
- Do NOT delete ingest_deputies.py itself — keep as dead code reference
- Renumber subsequent step comments if they exist (7→6, 8→7, etc.) — or just leave them with a gap and note

**1b. Add `chamber` to deputies index API:**
- In `packages/web/server/api/deputies/index.get.ts`, find the `db.select({...})` call
- Add `chamber: actors.chamber` to the select projection (alongside fullName, group, etc.)
- No other changes needed — `v-bind="deputy"` in index.vue spreads all fields to DeputyCard automatically

**1c. Add scrutins to sitemap:**
- In `packages/web/server/api/__sitemap__/urls.ts`:
  - Add `scrutins` to the import from `shared/schema` (alongside existing `debates` and `actors`)
  - Add a third query to the `Promise.all`: `db.select({ id: scrutins.id, date: scrutins.date }).from(scrutins).orderBy(asc(scrutins.id))`
  - Destructure as `scrutinRows` in the result
  - Map to URL objects: `const scrutinUrls = scrutinRows.map(s => ({ loc: \`/votes/\${s.id}\`, lastmod: s.date ? new Date(s.date).toISOString().split('T')[0] : undefined }))`
  - Add `...scrutinUrls` to the return array
  - If `asc` is not already imported from `drizzle-orm`, add it
  </action>
  <verify>
- `grep -n "skip.deputies\|ingest_deputies" packages/ingestion/scripts/run_all.py` should return only the comment line, not executable code
- `grep "chamber" packages/web/server/api/deputies/index.get.ts` shows `actors.chamber` in the select
- `grep "scrutins" packages/web/server/api/__sitemap__/urls.ts` shows the scrutins query and URL mapping
- `cd packages/web && npx nuxi typecheck 2>&1 | tail -20` — no type errors (or at minimum, no NEW errors in modified files)
  </verify>
  <done>
- run_all.py no longer runs ingest_deputies.py (Step 6 removed)
- Deputies list API returns chamber field for each actor
- Sitemap handler queries scrutins and returns /votes/:id URLs
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend fixes — render voteStats on profile + chamber badge on DeputyCard</name>
  <files>
    packages/web/app/pages/deputies/[id].vue
    packages/web/app/components/DeputyCard.vue
  </files>
  <action>
**2a. Render voteStats in deputies/[id].vue:**
- Find the section after `displayedTags` / tagStats (around line ~138) and before the interventions list (around line ~154)
- Add a voteStats section wrapped in `v-if="data.voteStats?.length > 0"`:

```html
<div v-if="data.voteStats?.length > 0" class="mb-6">
  <h2 class="text-sm font-semibold text-ink mb-2">Votes</h2>
  <div class="flex flex-wrap gap-2">
    <span
      v-for="stat in data.voteStats"
      :key="stat.position"
      class="px-3 py-1 rounded-full text-xs font-medium"
      :class="{
        'bg-green-100 text-green-800': stat.position === 'for',
        'bg-red-100 text-red-800': stat.position === 'against',
        'bg-gray-100 text-gray-600': stat.position === 'abstain',
        'bg-stone-100 text-stone-500': stat.position === 'absent',
      }"
    >
      {{ { for: 'Pour', against: 'Contre', abstain: 'Abstention', absent: 'Absent' }[stat.position] || stat.position }}
      <span class="ml-1 font-semibold">{{ stat.count }}</span>
    </span>
  </div>
</div>
```

- The position labels MUST be French: for→Pour, against→Contre, abstain→Abstention, absent→Absent
- Use inline mapping object (not a separate computed) — 4 values doesn't justify a utility
- Colors: green for Pour, red for Contre, gray for Abstention, muted stone for Absent

**2b. Add chamber badge to DeputyCard.vue:**
- Add `chamber?: string | null` to the `defineProps` type interface
- Find the location near the `GroupBadge` (or group display area) in the template
- Add chamber badge spans AFTER the group badge, reusing the exact pattern from deputies/[id].vue:

```html
<span
  v-if="chamber === 'AN'"
  class="text-xs bg-bronze/10 text-bronze px-2 py-0.5 rounded-full font-medium"
>AN</span>
<span
  v-else-if="chamber === 'Senat'"
  class="text-xs bg-ink/10 text-ink px-2 py-0.5 rounded-full font-medium"
>Sénat</span>
```

- Type `chamber` as optional (`?`) with `string | null` — safe if ever null, badge just won't render
- No changes needed in `pages/deputies/index.vue` — `v-bind="deputy"` spreads the new field automatically
  </action>
  <verify>
- `grep -n "voteStats" packages/web/app/pages/deputies/[id].vue` shows the rendering section
- `grep -n "chamber" packages/web/app/components/DeputyCard.vue` shows the prop and badge spans
- `cd packages/web && npx nuxi typecheck 2>&1 | tail -20` — no type errors in modified files
  </verify>
  <done>
- Deputy profile page renders voteStats with French labels and position-specific colors
- voteStats section hidden when empty (v-if guard)
- DeputyCard shows AN badge (bronze) or Senat badge (ink) based on chamber prop
- Chamber badge uses same design tokens as the detail page badge
  </done>
</task>

</tasks>

<verification>
All 4 gap closure items verified:

1. **Legacy script**: `python packages/ingestion/scripts/run_all.py --help` does NOT show `--skip-deputies` flag, and `grep ingest_deputies packages/ingestion/scripts/run_all.py` returns only the deprecation comment
2. **voteStats rendered**: Template contains voteStats section with French position labels (Pour/Contre/Abstention/Absent)
3. **Chamber badge**: DeputyCard has `chamber` prop and renders AN/Senat badge; index.get.ts selects `actors.chamber`
4. **Sitemap scrutins**: urls.ts queries scrutins table and maps to `/votes/:id` URLs

Type-check: `cd packages/web && npx nuxi typecheck` passes without new errors.
</verification>

<success_criteria>
- run_all.py executes full pipeline without touching non-existent deputies table
- Deputy profile shows vote position breakdown (4 categories, French labels, colored badges)
- Deputy cards in grid display chamber badge (AN bronze, Senat ink)
- Sitemap response includes /votes/:id URLs for all ~7062 scrutins
- No TypeScript errors introduced in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/16-cleanup-polish-v2/16-01-SUMMARY.md`
</output>
